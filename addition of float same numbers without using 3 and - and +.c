//c program to addition of float same numbers without using 3 and - and +
#include<stdio.h>
int main()
{
    float x,sum;
    printf("Enter the Number: ");
    scanf("%f",&x);
    sum=2*x;
    printf("Sum is %f",sum);
    return 0;
}